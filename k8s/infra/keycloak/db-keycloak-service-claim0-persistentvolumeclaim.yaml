apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    io.mad.service: db-keycloak-service-claim0
  name: db-keycloak-service-claim0
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi
