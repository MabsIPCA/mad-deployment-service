apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    io.mad.service: minio-claim0
  name: minio-claim0
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 100Mi