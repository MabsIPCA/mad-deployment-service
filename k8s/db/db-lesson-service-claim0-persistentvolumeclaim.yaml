apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    io.mad.service: db-lesson-service-claim0
  name: db-lesson-service-claim0
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 5Gi