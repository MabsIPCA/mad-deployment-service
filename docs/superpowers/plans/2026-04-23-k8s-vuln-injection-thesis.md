# K8s Vulnerability Thesis Section — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Write `thesis/kubernetes-vulns/kubernetes-vulns.tex` — a standalone LaTeX section covering all 20 OWASP K8s Top 10 (2025) entries injected into MAD Goat — plus bibliography, code snippets, and a table-generation script.

**Architecture:** `kubernetes-vulns.tex` is a `\section` file `\input`'d by the parent thesis. It `\input`s three table files from `tables/`. Code snippets live in `snippets/` for `\inputminted`. `gen-tables.sh` reads `data/vulnerabilities.yaml` via `yq` v4 to produce Tables 2 & 3. All compilation runs from `thesis/kubernetes-vulns/`.

**Tech Stack:** LaTeX + minted package, `pdflatex -shell-escape`, bash + yq v4 + awk, IEEE BibTeX (`ieeetr`)

---

## File Map

**Create:**
- `thesis/kubernetes-vulns/kubernetes-vulns.tex`
- `thesis/kubernetes-vulns/references.bib`
- `thesis/kubernetes-vulns/test-main.tex`
- `thesis/kubernetes-vulns/scripts/gen-tables.sh`
- `thesis/kubernetes-vulns/tables/crosswalk-2022-2025.tex`
- `thesis/kubernetes-vulns/tables/vuln-inventory.tex` *(generated by gen-tables.sh)*
- `thesis/kubernetes-vulns/tables/scanner-coverage.tex` *(generated by gen-tables.sh)*
- `thesis/kubernetes-vulns/snippets/k01-runAsRoot-vuln.yaml`
- `thesis/kubernetes-vulns/snippets/k01-runAsRoot-safe.yaml`
- `thesis/kubernetes-vulns/snippets/k01-readOnlyRootFs-vuln.yaml`
- `thesis/kubernetes-vulns/snippets/k01-readOnlyRootFs-safe.yaml`
- `thesis/kubernetes-vulns/snippets/k01-privileged-vuln.yaml`
- `thesis/kubernetes-vulns/snippets/k01-privileged-safe.yaml`
- `thesis/kubernetes-vulns/snippets/k01-unboundedResources-vuln.yaml`
- `thesis/kubernetes-vulns/snippets/k01-unboundedResources-safe.yaml`
- `thesis/kubernetes-vulns/snippets/k02-clusterAdminBinding-vuln.yaml`
- `thesis/kubernetes-vulns/snippets/k02-clusterAdminBinding-safe.yaml`
- `thesis/kubernetes-vulns/snippets/k02-secretsListWatch-vuln.yaml`
- `thesis/kubernetes-vulns/snippets/k02-secretsListWatch-safe.yaml`
- `thesis/kubernetes-vulns/snippets/k03-plaintextConfigMap-vuln.yaml`
- `thesis/kubernetes-vulns/snippets/k03-plaintextConfigMap-safe.yaml`
- `thesis/kubernetes-vulns/snippets/k03-jwtKeyInEnv-vuln.yaml`
- `thesis/kubernetes-vulns/snippets/k03-jwtKeyInEnv-safe.yaml`
- `thesis/kubernetes-vulns/snippets/k04-noPSA-vuln.yaml`
- `thesis/kubernetes-vulns/snippets/k04-noPSA-safe.yaml`
- `thesis/kubernetes-vulns/snippets/k04-weakKyverno-vuln.yaml`
- `thesis/kubernetes-vulns/snippets/k04-weakKyverno-safe.yaml`
- `thesis/kubernetes-vulns/snippets/k05-noDefaultDeny-vuln.yaml`
- `thesis/kubernetes-vulns/snippets/k05-noDefaultDeny-safe.yaml`
- `thesis/kubernetes-vulns/snippets/k05-permissiveNetpol-vuln.yaml`
- `thesis/kubernetes-vulns/snippets/k05-permissiveNetpol-safe.yaml`
- `thesis/kubernetes-vulns/snippets/k05-noEgressControls-vuln.yaml`
- `thesis/kubernetes-vulns/snippets/k05-noEgressControls-safe.yaml`
- `thesis/kubernetes-vulns/snippets/k06-traefikDashboard-vuln.yaml`
- `thesis/kubernetes-vulns/snippets/k06-rabbitmqMgmt-vuln.yaml`
- `thesis/kubernetes-vulns/snippets/k07-kubelet-vuln.yaml`
- `thesis/kubernetes-vulns/snippets/k07-kubelet-safe.yaml`
- `thesis/kubernetes-vulns/snippets/k09-keycloak-vuln.yaml`
- `thesis/kubernetes-vulns/snippets/k09-keycloak-safe.yaml`
- `thesis/kubernetes-vulns/snippets/k09-sharedSA-vuln.yaml`
- `thesis/kubernetes-vulns/snippets/k09-sharedSA-safe.yaml`
- `thesis/kubernetes-vulns/snippets/k10-auditPolicy-exploit.sh`
- `thesis/kubernetes-vulns/snippets/k10-auditPolicy-safe.yaml`

**Existing (do not touch):**
- `thesis/kubernetes-vulns/data/vulnerabilities.yaml`
- `scripts/exploits/*.sh`

---

### Task 0: Scaffold directories and README

**Files:**
- Create: `thesis/kubernetes-vulns/README.md`
- Create dirs: `thesis/kubernetes-vulns/snippets/`, `thesis/kubernetes-vulns/tables/`, `thesis/kubernetes-vulns/scripts/`

- [ ] **Step 0.1: Create directory structure**

```bash
mkdir -p thesis/kubernetes-vulns/snippets \
         thesis/kubernetes-vulns/tables \
         thesis/kubernetes-vulns/scripts
```

Run from: `.worktrees/k8s-vuln-injection/`
Expected: No output (directories created silently)

- [ ] **Step 0.2: Write README**

Create `thesis/kubernetes-vulns/README.md`:

```markdown
# kubernetes-vulns — Thesis Section

LaTeX source for the K8s vulnerability extension of the MAD Goat benchmark.

## Compilation

```bash
cd thesis/kubernetes-vulns
bash scripts/gen-tables.sh
pdflatex -shell-escape test-main.tex
```

## Structure

| Path | Purpose |
|------|---------|
| `kubernetes-vulns.tex` | Main section file (`\input`'d by parent thesis) |
| `references.bib` | IEEE BibTeX bibliography |
| `data/vulnerabilities.yaml` | Ground-truth data for Tables 2 & 3 |
| `scripts/gen-tables.sh` | Generates `tables/vuln-inventory.tex` and `tables/scanner-coverage.tex` |
| `tables/crosswalk-2022-2025.tex` | Table 1 — OWASP 2022↔2025 crosswalk (static) |
| `tables/vuln-inventory.tex` | Table 2 — vulnerability inventory (generated) |
| `tables/scanner-coverage.tex` | Table 3 — scanner coverage (generated) |
| `snippets/` | YAML/shell files for `\inputminted` in the tex |
| `test-main.tex` | Minimal wrapper for standalone compilation test |
```
```

- [ ] **Step 0.3: Verify directories exist**

```bash
ls thesis/kubernetes-vulns/
```

Expected output includes: `data/  README.md  scripts/  snippets/  tables/`

- [ ] **Step 0.4: Commit**

```bash
git add thesis/kubernetes-vulns/README.md
git commit -m "chore: scaffold thesis/kubernetes-vulns directory structure"
```

---

### Task 1: gen-tables.sh

**Files:**
- Create: `thesis/kubernetes-vulns/scripts/gen-tables.sh`

- [ ] **Step 1.1: Write gen-tables.sh**

Create `thesis/kubernetes-vulns/scripts/gen-tables.sh`:

```bash
#!/usr/bin/env bash
set -euo pipefail

SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
DATA="$SCRIPT_DIR/../data/vulnerabilities.yaml"
OUT="$SCRIPT_DIR/../tables"

# Table 2: Vulnerability Inventory
{
cat <<'HEADER'
% Generated by scripts/gen-tables.sh — do not edit manually
\begin{longtable}{@{}lp{5cm}cclll@{}}
\caption{MAD Goat Kubernetes Vulnerability Inventory}\label{tab:vuln-inventory}\\
\toprule
\textbf{ID} & \textbf{Title} & \textbf{2025} & \textbf{2022} & \textbf{CWE} & \textbf{CIS} & \textbf{Scope} \\
\midrule
\endfirsthead
\multicolumn{7}{c}{\tablename\ \thetable{} (continued)}\\
\toprule
\textbf{ID} & \textbf{Title} & \textbf{2025} & \textbf{2022} & \textbf{CWE} & \textbf{CIS} & \textbf{Scope} \\
\midrule
\endhead
\midrule
\multicolumn{7}{r}{\textit{Continued on next page}}\\
\endfoot
\bottomrule
\endlastfoot
HEADER

yq -r '.[] | [
  .id,
  .title,
  .owasp2025,
  (if .owasp2022 == "" then "---" else .owasp2022 end),
  ("CWE-" + (.cwe | tostring)),
  (if .cis == "" then "---" else .cis end),
  .scope
] | @tsv' "$DATA" | \
awk -F'\t' '{
  gsub(/_/, "\\_", $1)
  gsub(/_/, "\\_", $2)
  printf "\\texttt{%s} & %s & %s & %s & %s & %s & %s \\\\\n", $1, $2, $3, $4, $5, $6, $7
}'

echo '\end{longtable}'
} > "$OUT/vuln-inventory.tex"

# Table 3: Scanner Coverage
{
cat <<'HEADER'
% Generated by scripts/gen-tables.sh — do not edit manually
\begin{longtable}{@{}lp{3.2cm}p{2cm}p{2.4cm}p{2cm}@{}}
\caption{Static Analysis Scanner Coverage per Vulnerability}\label{tab:scanner-coverage}\\
\toprule
\textbf{ID} & \textbf{KICS} & \textbf{Trivy} & \textbf{KubeLinter} & \textbf{Kubescape} \\
\midrule
\endfirsthead
\multicolumn{5}{c}{\tablename\ \thetable{} (continued)}\\
\toprule
\textbf{ID} & \textbf{KICS} & \textbf{Trivy} & \textbf{KubeLinter} & \textbf{Kubescape} \\
\midrule
\endhead
\midrule
\multicolumn{5}{r}{\textit{Continued on next page}}\\
\endfoot
\bottomrule
\endlastfoot
HEADER

yq -r '.[] | [
  .id,
  (if .scanners.kics == "" then "---" else .scanners.kics end),
  (if .scanners.trivy == "" then "---" else .scanners.trivy end),
  (if .scanners.kubelinter == "" then "---" else .scanners.kubelinter end),
  (if .scanners.kubescape == "" then "---" else .scanners.kubescape end)
] | @tsv' "$DATA" | \
awk -F'\t' '{
  gsub(/_/, "\\_", $1)
  # Truncate KICS UUIDs to first 8 chars for table width
  if (length($2) > 8 && $2 != "---") $2 = substr($2, 1, 8) "\\ldots"
  printf "\\texttt{%s} & \\texttt{\\scriptsize %s} & \\texttt{%s} & \\texttt{\\scriptsize %s} & \\texttt{%s} \\\\\n", $1, $2, $3, $4, $5
}'

echo '\end{longtable}'
} > "$OUT/scanner-coverage.tex"

echo "Generated: $OUT/vuln-inventory.tex"
echo "Generated: $OUT/scanner-coverage.tex"
```

- [ ] **Step 1.2: Make executable**

```bash
chmod +x thesis/kubernetes-vulns/scripts/gen-tables.sh
```

- [ ] **Step 1.3: Run gen-tables.sh to verify output**

```bash
cd "C:/Users/miabs/mad-deployment-service/.worktrees/k8s-vuln-injection"
bash thesis/kubernetes-vulns/scripts/gen-tables.sh
```

Expected:
```
Generated: .../tables/vuln-inventory.tex
Generated: .../tables/scanner-coverage.tex
```

- [ ] **Step 1.4: Spot-check output**

```bash
head -5 thesis/kubernetes-vulns/tables/vuln-inventory.tex
grep "k01" thesis/kubernetes-vulns/tables/vuln-inventory.tex | head -2
```

Expected: LaTeX longtable header, then rows with `k01-runAsRoot` and `k01-readOnlyRootFs`.

- [ ] **Step 1.5: Commit**

```bash
git add thesis/kubernetes-vulns/scripts/gen-tables.sh \
        thesis/kubernetes-vulns/tables/vuln-inventory.tex \
        thesis/kubernetes-vulns/tables/scanner-coverage.tex
git commit -m "feat: add gen-tables.sh and generate Tables 2 & 3 from vulnerabilities.yaml"
```

---

### Task 2: Table 1 — OWASP 2022↔2025 Crosswalk (static)

**Files:**
- Create: `thesis/kubernetes-vulns/tables/crosswalk-2022-2025.tex`

- [ ] **Step 2.1: Write crosswalk table**

Create `thesis/kubernetes-vulns/tables/crosswalk-2022-2025.tex`:

```latex
% Table 1: OWASP Kubernetes Top 10 crosswalk, 2022 vs. 2025
\begin{table}[ht]
\centering
\caption{OWASP Kubernetes Top~10 Crosswalk: 2022 vs.\ 2025~\cite{owasp-k8s-2025,owasp-k8s-2022}}
\label{tab:owasp-crosswalk}
\begin{tabular}{@{}clcl@{}}
\toprule
\textbf{2025} & \textbf{2025 Title} & \textbf{2022} & \textbf{2022 Title} \\
\midrule
K01 & Insecure Workload Configurations     & K01 & Insecure Workload Configurations \\
K02 & Overly Permissive RBAC               & K03 & Overly Permissive RBAC \\
K03 & Secrets Management Failures          & K08 & Secrets Management Failures \\
K04 & Lack of Centralised Policy Enforcement & K04 & Policy Enforcement \\
K05 & Inadequate Network Segmentation      & K07 & Network Segmentation Failures \\
K06 & Broken Authentication Mechanisms     & ---  & \textit{(new in 2025)} \\
K07 & Misconfigured Cluster Components     & K09 & Misconfigured Cluster Components \\
K08 & Supply Chain Vulnerabilities         & K02 & Supply Chain Vulnerabilities \\
K09 & Misconfigured Authentication Defaults & K06 & Broken Authentication \\
K10 & Inadequate Audit Logging             & K05 & Inadequate Logging \\
\bottomrule
\end{tabular}
\end{table}
```

- [ ] **Step 2.2: Verify**

```bash
grep "label{tab:owasp-crosswalk}" thesis/kubernetes-vulns/tables/crosswalk-2022-2025.tex
```

Expected: `\label{tab:owasp-crosswalk}`

- [ ] **Step 2.3: Commit**

```bash
git add thesis/kubernetes-vulns/tables/crosswalk-2022-2025.tex
git commit -m "feat: add static OWASP crosswalk table (Table 1)"
```

---

### Task 3: references.bib

**Files:**
- Create: `thesis/kubernetes-vulns/references.bib`

- [ ] **Step 3.1: Write references.bib**

Create `thesis/kubernetes-vulns/references.bib`:

```bibtex
@misc{owasp-k8s-2025,
  author       = {{OWASP Foundation}},
  title        = {{OWASP} {K}ubernetes {T}op~10 (2025)},
  year         = {2025},
  howpublished = {\url{https://owasp.org/www-project-kubernetes-top-ten/}},
  note         = {Accessed: 2026-04-22}
}

@misc{owasp-k8s-2022,
  author       = {{OWASP Foundation}},
  title        = {{OWASP} {K}ubernetes {T}op~10 (2022)},
  year         = {2022},
  howpublished = {\url{https://owasp.org/www-project-kubernetes-top-ten/2022/}},
  note         = {Accessed: 2026-04-22}
}

@techreport{cis-k8s-1.9,
  author      = {{Center for Internet Security}},
  title       = {{CIS} {K}ubernetes {B}enchmark v1.9.0},
  institution = {Center for Internet Security},
  year        = {2024},
  type        = {Benchmark}
}

@techreport{nist-sp800-190,
  author      = {Souppaya, Murugiah and Morello, John and Scarfone, Karen},
  title       = {Application Container Security Guide},
  institution = {National Institute of Standards and Technology},
  year        = {2017},
  number      = {NIST SP~800-190},
  doi         = {10.6028/NIST.SP.800-190}
}

@techreport{nsa-cisa-k8s-2022,
  author      = {{National Security Agency} and {Cybersecurity and Infrastructure Security Agency}},
  title       = {{K}ubernetes Hardening Guidance, Revision~1.2},
  institution = {NSA/CISA},
  year        = {2022},
  type        = {Technical Report}
}

@misc{k8s-psa-docs,
  author       = {{The Kubernetes Authors}},
  title        = {Pod Security Admission},
  year         = {2024},
  howpublished = {\url{https://kubernetes.io/docs/concepts/security/pod-security-admission/}},
  note         = {Accessed: 2026-04-22}
}

@misc{k8s-netpol-docs,
  author       = {{The Kubernetes Authors}},
  title        = {Network Policies},
  year         = {2024},
  howpublished = {\url{https://kubernetes.io/docs/concepts/services-networking/network-policies/}},
  note         = {Accessed: 2026-04-22}
}

@misc{k8s-rbac-docs,
  author       = {{The Kubernetes Authors}},
  title        = {Using {RBAC} Authorization},
  year         = {2024},
  howpublished = {\url{https://kubernetes.io/docs/reference/access-authn-authz/rbac/}},
  note         = {Accessed: 2026-04-22}
}

@misc{k8s-audit-docs,
  author       = {{The Kubernetes Authors}},
  title        = {Auditing},
  year         = {2024},
  howpublished = {\url{https://kubernetes.io/docs/tasks/debug/debug-cluster/audit/}},
  note         = {Accessed: 2026-04-22}
}

@misc{k8s-secrets-docs,
  author       = {{The Kubernetes Authors}},
  title        = {Secrets},
  year         = {2024},
  howpublished = {\url{https://kubernetes.io/docs/concepts/configuration/secret/}},
  note         = {Accessed: 2026-04-22}
}

@misc{k8s-kubelet-docs,
  author       = {{The Kubernetes Authors}},
  title        = {{K}ubelet Configuration (\texttt{KubeletConfiguration})},
  year         = {2024},
  howpublished = {\url{https://kubernetes.io/docs/reference/config-api/kubelet-config.v1beta1/}},
  note         = {Accessed: 2026-04-22}
}

@misc{kyverno-docs,
  author       = {{Kyverno Authors}},
  title        = {Kyverno Policy Engine Documentation},
  year         = {2024},
  howpublished = {\url{https://kyverno.io/docs/}},
  note         = {Accessed: 2026-04-22}
}

@misc{trivy-k8s,
  author       = {{Aqua Security}},
  title        = {Trivy: {A} Comprehensive Security Scanner},
  year         = {2024},
  howpublished = {\url{https://aquasecurity.github.io/trivy/}},
  note         = {Accessed: 2026-04-22}
}

@misc{kics-tool,
  author       = {{Checkmarx}},
  title        = {{KICS}: Keeping Infrastructure as Code Secure},
  year         = {2024},
  howpublished = {\url{https://kics.io}},
  note         = {Accessed: 2026-04-22}
}

@misc{kubelinter,
  author       = {{StackRox}},
  title        = {{KubeLinter}: Static Analysis for {K}ubernetes},
  year         = {2024},
  howpublished = {\url{https://github.com/stackrox/kube-linter}},
  note         = {Accessed: 2026-04-22}
}

@misc{kubescape,
  author       = {{ARMO}},
  title        = {Kubescape: {K}ubernetes Security Platform},
  year         = {2024},
  howpublished = {\url{https://kubescape.io}},
  note         = {Accessed: 2026-04-22}
}

@misc{mad-goat-repo,
  author       = {{MAD Goat Project}},
  title        = {{MAD} {G}oat: Microservice Architecture Demonstration for Security Benchmarking},
  year         = {2024},
  howpublished = {\url{https://github.com/MAD-Goat-Project/mad-deployment-service}},
  note         = {Accessed: 2026-04-22}
}

@inproceedings{zerouali-2023,
  author    = {Zerouali, Ahmed and Mens, Tom and Rocha, Henrique and De Roover, Coen},
  title     = {On the Relation Between Outdated Docker Containers, Outdated Image Dependencies, and New Vulnerabilities},
  booktitle = {Proc.\ IEEE/ACM 20th Int.\ Conf.\ Mining Software Repositories (MSR)},
  year      = {2023},
  pages     = {297--308},
  doi       = {10.1109/MSR59073.2023.00049}
}

@inproceedings{rahman-2019,
  author    = {Rahman, Akond and Parnin, Chris and Williams, Laurie},
  title     = {The Seven Sins: Security Smells in Infrastructure as Code Scripts},
  booktitle = {Proc.\ 41st Int.\ Conf.\ Software Engineering (ICSE)},
  year      = {2019},
  pages     = {164--175},
  doi       = {10.1109/ICSE.2019.00033}
}

@inproceedings{shamim-2020,
  author    = {Shamim, Md Shazibul Islam and Bhuiyan, Farzana Ahamed and Rahman, Akond},
  title     = {{XI} Commandments of {K}ubernetes Security: {A} Systematization of Knowledge Related to {K}ubernetes Security Practices},
  booktitle = {Proc.\ IEEE Secure Development Conf.\ (SecDev)},
  year      = {2020},
  pages     = {58--64},
  doi       = {10.1109/SecDev45635.2020.00025}
}

@inproceedings{bose-2023,
  author    = {Bose, Dibyendu Brinto and Rahman, Akond and Shamim, Shazibul Islam},
  title     = {``Under-Reported'' Security Defects in {K}ubernetes Manifests},
  booktitle = {Proc.\ IEEE/ACM Workshop Engineering and Cybersecurity of Critical Systems (EnCyCriS)},
  year      = {2023},
  pages     = {9--12},
  doi       = {10.1109/EnCyCriS59283.2023.00007}
}

@misc{traefik-docs,
  author       = {{Traefik Labs}},
  title        = {Traefik Proxy Documentation},
  year         = {2024},
  howpublished = {\url{https://doc.traefik.io/traefik/}},
  note         = {Accessed: 2026-04-22}
}

@misc{keycloak-docs,
  author       = {{Red Hat}},
  title        = {Keycloak Documentation},
  year         = {2024},
  howpublished = {\url{https://www.keycloak.org/documentation}},
  note         = {Accessed: 2026-04-22}
}

@misc{aws-imds,
  author       = {{Amazon Web Services}},
  title        = {{EC2} Instance Metadata Service},
  year         = {2024},
  howpublished = {\url{https://docs.aws.amazon.com/AWSEC2/latest/UserGuide/ec2-instance-metadata.html}},
  note         = {Accessed: 2026-04-22}
}

@misc{azure-imds,
  author       = {{Microsoft Azure}},
  title        = {Azure Instance Metadata Service},
  year         = {2024},
  howpublished = {\url{https://learn.microsoft.com/en-us/azure/virtual-machines/instance-metadata-service}},
  note         = {Accessed: 2026-04-22}
}

@misc{helm-docs,
  author       = {{Helm Authors}},
  title        = {Helm: The Package Manager for {K}ubernetes},
  year         = {2024},
  howpublished = {\url{https://helm.sh/docs/}},
  note         = {Accessed: 2026-04-22}
}
```

- [ ] **Step 3.2: Count entries**

```bash
grep -c "^@" thesis/kubernetes-vulns/references.bib
```

Expected: `27`

- [ ] **Step 3.3: Commit**

```bash
git add thesis/kubernetes-vulns/references.bib
git commit -m "feat: add references.bib with 27 IEEE BibTeX entries"
```

---

### Task 4: kubernetes-vulns.tex — Opening (Motivation + Methodology)

**Files:**
- Create: `thesis/kubernetes-vulns/kubernetes-vulns.tex`
- Create: `thesis/kubernetes-vulns/test-main.tex`

- [ ] **Step 4.1: Write test-main.tex**

Create `thesis/kubernetes-vulns/test-main.tex`:

```latex
\documentclass[12pt,a4paper]{article}
\usepackage[T1]{fontenc}
\usepackage[utf8]{inputenc}
\usepackage{booktabs}
\usepackage{longtable}
\usepackage{minted}
\usepackage{caption}
\usepackage{hyperref}
\usepackage{url}
\usepackage{float}
\usepackage{newfloat}
\DeclareFloatingEnvironment[name=Listing]{listing}

\begin{document}
\input{kubernetes-vulns}
\bibliographystyle{ieeetr}
\bibliography{references}
\end{document}
```

- [ ] **Step 4.2: Create kubernetes-vulns.tex with opening content**

Create `thesis/kubernetes-vulns/kubernetes-vulns.tex`:

```latex
% kubernetes-vulns.tex
% Thesis section: Extending MAD Goat to the Kubernetes Layer
% Compiled from thesis/kubernetes-vulns/ with:
%   pdflatex -shell-escape test-main.tex

\section{Extending the MAD Goat Benchmark to the Kubernetes Layer}
\label{sec:k8s-vulns}

\subsection{Motivation and Context}
\label{sec:k8s-motivation}

The MAD~Goat benchmark~\cite{mad-goat-repo} was originally designed to surface web-application and microservice-level vulnerabilities in containerised workloads.
As Kubernetes adoption has grown, the attack surface has shifted from individual containers to the orchestration layer itself: cluster RBAC, admission controllers, network policies, kubelet configuration, and audit infrastructure all introduce exploitable misconfigurations that are invisible to application-layer scanners~\cite{shamim-2020,bose-2023}.
The OWASP Kubernetes Top~10 2025 edition~\cite{owasp-k8s-2025} codifies ten categories of such weaknesses, updating and renumbering the 2022 list~\cite{owasp-k8s-2022} (Table~\ref{tab:owasp-crosswalk}).
This section extends MAD~Goat to the Kubernetes layer by injecting all ten categories into the benchmark's Helm charts and cluster bootstrap configuration, providing a reproducible, toggle-controlled environment for evaluating both active exploits and static analysis tools.

\input{tables/crosswalk-2022-2025}

\subsection{Methodology}
\label{sec:k8s-methodology}

Each vulnerability entry is controlled by a boolean flag in the chart's \texttt{values.yaml} (default: \texttt{true} = vulnerable).
Chart-level entries (\texttt{scope: chart}) live in \texttt{helm/madgoat} or \texttt{helm/madgoat-infra} and can be toggled with \texttt{helm upgrade}.
Cluster-level entries (\texttt{scope: cluster}) are applied via \texttt{kubectl apply -k k8s/insecure-defaults/} and require a full Minikube restart to remediate; the \texttt{make reset-safe} target automates this.
Kubelet flags (K07) are passed as \texttt{minikube start -{}-extra-config} arguments and are therefore not Helm-controlled.
Two entries (K08) are conceptual only: the IMDS credential-theft attack surface does not exist in a local Minikube cluster and is documented with references to cloud-vendor behaviour~\cite{aws-imds,azure-imds}.

For each \emph{full}-class entry the benchmark provides:
\begin{itemize}
  \item A \texttt{values.yaml} boolean flag that injects or suppresses the misconfiguration.
  \item A verification script in \texttt{scripts/exploits/} that exits~0 (PASS) when the vulnerable condition is confirmed and exits~1 (FAIL/safe) otherwise.
  \item Code snippets (Listings below) illustrating the vulnerable and remediated manifests.
\end{itemize}

The ground-truth inventory is maintained in \texttt{thesis/kubernetes-vulns/data/vulnerabilities.yaml}; Tables~\ref{tab:vuln-inventory} and~\ref{tab:scanner-coverage} are generated from it automatically by \texttt{scripts/gen-tables.sh}~\cite{trivy-k8s,kics-tool,kubelinter,kubescape}.

\input{tables/vuln-inventory}

\subsection{Vulnerability Catalogue}
\label{sec:k8s-catalogue}
```

- [ ] **Step 4.3: Verify opening compiles (syntax check only)**

```bash
cd "C:/Users/miabs/mad-deployment-service/.worktrees/k8s-vuln-injection/thesis/kubernetes-vulns"
grep -c "\\\\label" kubernetes-vulns.tex
```

Expected: `4` (sec:k8s-vulns, sec:k8s-motivation, sec:k8s-methodology, sec:k8s-catalogue)

- [ ] **Step 4.4: Commit**

```bash
cd "C:/Users/miabs/mad-deployment-service/.worktrees/k8s-vuln-injection"
git add thesis/kubernetes-vulns/kubernetes-vulns.tex \
        thesis/kubernetes-vulns/test-main.tex
git commit -m "feat: add kubernetes-vulns.tex opening — motivation and methodology"
```

---

### Task 5: K01 — Insecure Workload Configurations

**Files:**
- Create: `thesis/kubernetes-vulns/snippets/k01-*.yaml` (8 files)
- Modify: `thesis/kubernetes-vulns/kubernetes-vulns.tex` (append)

- [ ] **Step 5.1: Write K01 snippet files**

Create `thesis/kubernetes-vulns/snippets/k01-runAsRoot-vuln.yaml`:
```yaml
# K01-a vulnerable: k01_runAsRoot=true (default)
# Container runs as UID 0 — no securityContext hardening applied
spec:
  containers:
    - name: webapp
      image: ghcr.io/mad-goat-project/webapp:latest
      # securityContext absent → root user, privilege escalation permitted
```

Create `thesis/kubernetes-vulns/snippets/k01-runAsRoot-safe.yaml`:
```yaml
# K01-a remediated: k01_runAsRoot=false
spec:
  containers:
    - name: webapp
      image: ghcr.io/mad-goat-project/webapp:latest
      securityContext:
        runAsNonRoot: true
        runAsUser: 10001
        allowPrivilegeEscalation: false
```

Create `thesis/kubernetes-vulns/snippets/k01-readOnlyRootFs-vuln.yaml`:
```yaml
# K01-b vulnerable: k01_readOnlyRootFs=true (default)
# readOnlyRootFilesystem absent — attacker can modify container binaries
spec:
  containers:
    - name: webapp
      securityContext: {}  # readOnlyRootFilesystem implicitly false
```

Create `thesis/kubernetes-vulns/snippets/k01-readOnlyRootFs-safe.yaml`:
```yaml
# K01-b remediated: k01_readOnlyRootFs=false
spec:
  containers:
    - name: webapp
      securityContext:
        readOnlyRootFilesystem: true
```

Create `thesis/kubernetes-vulns/snippets/k01-privileged-vuln.yaml`:
```yaml
# K01-c vulnerable: k01_privileged=true (madgoat chart only)
# privileged=true grants full host kernel access from the container
spec:
  containers:
    - name: mad4shell-unsafe
      securityContext:
        privileged: true
```

Create `thesis/kubernetes-vulns/snippets/k01-privileged-safe.yaml`:
```yaml
# K01-c remediated: k01_privileged=false
# privileged field absent in generated manifest → false by default
spec:
  containers:
    - name: mad4shell-unsafe
      securityContext:
        privileged: false
```

Create `thesis/kubernetes-vulns/snippets/k01-unboundedResources-vuln.yaml`:
```yaml
# K01-d vulnerable: k01_unboundedResources=true (lesson pod)
# No CPU/memory limits — pod can starve other workloads (DoS)
spec:
  containers:
    - name: lesson
      # resources: field absent → no limits enforced by scheduler
```

Create `thesis/kubernetes-vulns/snippets/k01-unboundedResources-safe.yaml`:
```yaml
# K01-d remediated: k01_unboundedResources=false
spec:
  containers:
    - name: lesson
      resources:
        requests:
          cpu: "100m"
          memory: "128Mi"
        limits:
          cpu: "500m"
          memory: "512Mi"
```

- [ ] **Step 5.2: Append K01 LaTeX content to kubernetes-vulns.tex**

Append to `thesis/kubernetes-vulns/kubernetes-vulns.tex`:

```latex

\subsubsection{K01 --- Insecure Workload Configurations (2022: K01)}
\label{sec:k01}

K01 covers pod-level security defaults that widen the blast radius of any container compromise.
MAD~Goat injects four orthogonal K01 sub-entries across both charts via independent boolean flags.

\paragraph{K01-a: Run-as-root containers (CWE-250, CIS~5.2.6).}\label{sec:k01-runAsRoot}
When no \texttt{securityContext} is applied, Kubernetes allows containers to execute as \textsc{uid}~0.
A successful container escape from a root process gains equivalent privileges on the host filesystem.
The flag \texttt{vulnerabilities.k01\_runAsRoot=true} (default) suppresses the security context on every pod in both charts; setting it to \texttt{false} injects \texttt{runAsNonRoot: true}, \texttt{runAsUser: 10001}, and \texttt{allowPrivilegeEscalation: false}~\cite{nsa-cisa-k8s-2022,cis-k8s-1.9}.
Listings~\ref{lst:k01-runAsRoot-vuln} and~\ref{lst:k01-runAsRoot-safe} show the generated difference.
Static scanners detect this pattern: KICS rule \texttt{cf34805e}, Trivy \texttt{KSV012}, KubeLinter \texttt{run-as-non-root}, Kubescape \texttt{C-0013}~\cite{trivy-k8s,kics-tool,kubelinter,kubescape}.

\begin{listing}[ht]
\inputminted[fontsize=\footnotesize,frame=lines]{yaml}{snippets/k01-runAsRoot-vuln.yaml}
\caption{K01-a vulnerable: \texttt{securityContext} absent — container runs as root.}\label{lst:k01-runAsRoot-vuln}
\end{listing}

\begin{listing}[ht]
\inputminted[fontsize=\footnotesize,frame=lines]{yaml}{snippets/k01-runAsRoot-safe.yaml}
\caption{K01-a remediated: \texttt{runAsNonRoot} and non-zero \texttt{runAsUser} enforced.}\label{lst:k01-runAsRoot-safe}
\end{listing}

\paragraph{K01-b: Writable root filesystem (CWE-276).}\label{sec:k01-readOnlyRootFs}
Without \texttt{readOnlyRootFilesystem: true}, an attacker with code execution can overwrite container binaries or drop persistence payloads.
The flag \texttt{vulnerabilities.k01\_readOnlyRootFs} controls this field on all pods in both charts.
Trivy check \texttt{KSV014}, KICS rule \texttt{a9c2f49d}, KubeLinter \texttt{read-only-root-fs}, and Kubescape \texttt{C-0017} all flag its absence~\cite{trivy-k8s,kics-tool}.

\begin{listing}[ht]
\inputminted[fontsize=\footnotesize,frame=lines]{yaml}{snippets/k01-readOnlyRootFs-vuln.yaml}
\caption{K01-b vulnerable: empty \texttt{securityContext} leaves filesystem writable.}\label{lst:k01-readOnlyRootFs-vuln}
\end{listing}

\begin{listing}[ht]
\inputminted[fontsize=\footnotesize,frame=lines]{yaml}{snippets/k01-readOnlyRootFs-safe.yaml}
\caption{K01-b remediated: \texttt{readOnlyRootFilesystem: true} applied.}\label{lst:k01-readOnlyRootFs-safe}
\end{listing}

\paragraph{K01-c: Privileged containers (CWE-250, CIS~5.2.2).}\label{sec:k01-privileged}
Setting \texttt{privileged: true} disables all Linux namespace isolation between the container and the host kernel~\cite{nist-sp800-190}.
MAD~Goat applies this to three services in \texttt{helm/madgoat} (\texttt{mad4shell-unsafe}, \texttt{profile}, \texttt{webapp}) that model intentionally dangerous images.
Trivy check \texttt{KSV017}, KICS rule \texttt{dd29336b}, KubeLinter \texttt{privileged-container}, and Kubescape \texttt{C-0016} detect this condition~\cite{trivy-k8s,kics-tool,kubelinter,kubescape}.

\begin{listing}[ht]
\inputminted[fontsize=\footnotesize,frame=lines]{yaml}{snippets/k01-privileged-vuln.yaml}
\caption{K01-c vulnerable: \texttt{privileged: true} on \texttt{mad4shell-unsafe}.}\label{lst:k01-privileged-vuln}
\end{listing}

\begin{listing}[ht]
\inputminted[fontsize=\footnotesize,frame=lines]{yaml}{snippets/k01-privileged-safe.yaml}
\caption{K01-c remediated: \texttt{privileged} absent (defaults to \texttt{false}).}\label{lst:k01-privileged-safe}
\end{listing}

\paragraph{K01-d: Unbounded resource usage (CWE-400).}\label{sec:k01-unboundedResources}
A container without \texttt{resources.limits} can consume unlimited CPU and memory, enabling denial-of-service against co-located workloads~\cite{nsa-cisa-k8s-2022}.
The flag \texttt{vulnerabilities.k01\_unboundedResources} controls limit injection on the \texttt{lesson} pod in \texttt{helm/madgoat}.
Trivy checks \texttt{KSV011}/\texttt{KSV018}, KICS rule \texttt{b14d1bc4}, and Kubescape \texttt{C-0009} detect this~\cite{trivy-k8s,kubescape}.

\begin{listing}[ht]
\inputminted[fontsize=\footnotesize,frame=lines]{yaml}{snippets/k01-unboundedResources-vuln.yaml}
\caption{K01-d vulnerable: \texttt{resources} field absent on \texttt{lesson} pod.}\label{lst:k01-unboundedResources-vuln}
\end{listing}

\begin{listing}[ht]
\inputminted[fontsize=\footnotesize,frame=lines]{yaml}{snippets/k01-unboundedResources-safe.yaml}
\caption{K01-d remediated: CPU and memory \texttt{requests}/\texttt{limits} applied.}\label{lst:k01-unboundedResources-safe}
\end{listing}
```

- [ ] **Step 5.3: Verify labels present**

```bash
grep "label{sec:k01" thesis/kubernetes-vulns/kubernetes-vulns.tex
```

Expected: 5 lines (sec:k01, sec:k01-runAsRoot, sec:k01-readOnlyRootFs, sec:k01-privileged, sec:k01-unboundedResources)

- [ ] **Step 5.4: Commit**

```bash
cd "C:/Users/miabs/mad-deployment-service/.worktrees/k8s-vuln-injection"
git add thesis/kubernetes-vulns/snippets/k01-*.yaml \
        thesis/kubernetes-vulns/kubernetes-vulns.tex
git commit -m "feat: add K01 snippets and LaTeX paragraphs"
```

---

### Task 6: K02 — Overly Permissive RBAC

**Files:**
- Create: `thesis/kubernetes-vulns/snippets/k02-*.yaml` (4 files)
- Modify: `thesis/kubernetes-vulns/kubernetes-vulns.tex` (append)

- [ ] **Step 6.1: Write K02 snippet files**

Create `thesis/kubernetes-vulns/snippets/k02-clusterAdminBinding-vuln.yaml`:
```yaml
# K02-a vulnerable: k02_clusterAdminBinding=true
# Default ServiceAccount bound to cluster-admin — any pod gets full cluster control
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRoleBinding
metadata:
  name: mad-cluster-admin
subjects:
  - kind: ServiceAccount
    name: default
    namespace: default
roleRef:
  kind: ClusterRole
  name: cluster-admin
  apiGroup: rbac.authorization.k8s.io
```

Create `thesis/kubernetes-vulns/snippets/k02-clusterAdminBinding-safe.yaml`:
```yaml
# K02-a remediated: k02_clusterAdminBinding=false
# ClusterRoleBinding not created — default SA retains no elevated permissions
# (Safe state = absence of the ClusterRoleBinding resource above)
```

Create `thesis/kubernetes-vulns/snippets/k02-secretsListWatch-vuln.yaml`:
```yaml
# K02-b vulnerable: k02_secretsListWatch=true
# Role grants list+watch on all Secrets — allows bulk credential harvesting
apiVersion: rbac.authorization.k8s.io/v1
kind: Role
metadata:
  name: mad-secrets-reader
  namespace: default
rules:
  - apiGroups: [""]
    resources: ["secrets"]
    verbs: ["get", "list", "watch"]
```

Create `thesis/kubernetes-vulns/snippets/k02-secretsListWatch-safe.yaml`:
```yaml
# K02-b remediated: k02_secretsListWatch=false
# Role not created — no principal has list/watch access to Secrets
# (Safe state = absence of the Role and RoleBinding resources above)
```

- [ ] **Step 6.2: Append K02 LaTeX content**

Append to `thesis/kubernetes-vulns/kubernetes-vulns.tex`:

```latex

\subsubsection{K02 --- Overly Permissive RBAC (2022: K03)}
\label{sec:k02}

Over-privileged RBAC bindings are the primary lateral-movement path in a compromised Kubernetes cluster~\cite{k8s-rbac-docs,shamim-2020}.
MAD~Goat injects two complementary RBAC misconfigurations into \texttt{helm/madgoat-infra}.

\paragraph{K02-a: Default ServiceAccount bound to \texttt{cluster-admin} (CWE-269, CIS~5.1.1).}\label{sec:k02-clusterAdminBinding}
Binding the \texttt{default} ServiceAccount to the built-in \texttt{cluster-admin} ClusterRole means any pod in the \texttt{default} namespace that mounts its service-account token can perform any action on any resource cluster-wide.
The flag \texttt{vulnerabilities.k02\_clusterAdminBinding=true} emits the ClusterRoleBinding in Listing~\ref{lst:k02-clusterAdminBinding-vuln}; setting it to \texttt{false} suppresses the resource entirely (Listing~\ref{lst:k02-clusterAdminBinding-safe}).
Detected by: Trivy \texttt{KSV111}, KICS rule \texttt{249328b8}, KubeLinter \texttt{cluster-admin-role-binding}, Kubescape \texttt{C-0035}~\cite{trivy-k8s,kics-tool,kubelinter,kubescape}.

\begin{listing}[ht]
\inputminted[fontsize=\footnotesize,frame=lines]{yaml}{snippets/k02-clusterAdminBinding-vuln.yaml}
\caption{K02-a vulnerable: \texttt{default} SA bound to \texttt{cluster-admin}.}\label{lst:k02-clusterAdminBinding-vuln}
\end{listing}

\begin{listing}[ht]
\inputminted[fontsize=\footnotesize,frame=lines]{yaml}{snippets/k02-clusterAdminBinding-safe.yaml}
\caption{K02-a remediated: ClusterRoleBinding absent.}\label{lst:k02-clusterAdminBinding-safe}
\end{listing}

\paragraph{K02-b: Role with \texttt{list}/\texttt{watch} on Secrets (CWE-732, CIS~5.1.3).}\label{sec:k02-secretsListWatch}
A Role granting \texttt{list} and \texttt{watch} verbs on the \texttt{secrets} resource enables any pod bound to that Role to enumerate and continuously watch all Secret objects in the namespace, effectively providing a bulk credential dump~\cite{k8s-rbac-docs}.
Trivy \texttt{KSV041}, KICS rule \texttt{b7bca5c4}, KubeLinter \texttt{access-to-secrets}, and Kubescape \texttt{C-0015} detect this~\cite{trivy-k8s,kics-tool,kubelinter,kubescape}.

\begin{listing}[ht]
\inputminted[fontsize=\footnotesize,frame=lines]{yaml}{snippets/k02-secretsListWatch-vuln.yaml}
\caption{K02-b vulnerable: Role permits \texttt{list}+\texttt{watch} on all Secrets.}\label{lst:k02-secretsListWatch-vuln}
\end{listing}

\begin{listing}[ht]
\inputminted[fontsize=\footnotesize,frame=lines]{yaml}{snippets/k02-secretsListWatch-safe.yaml}
\caption{K02-b remediated: Role absent.}\label{lst:k02-secretsListWatch-safe}
\end{listing}
```

- [ ] **Step 6.3: Verify**

```bash
grep "label{sec:k02" thesis/kubernetes-vulns/kubernetes-vulns.tex
```

Expected: 3 lines (sec:k02, sec:k02-clusterAdminBinding, sec:k02-secretsListWatch)

- [ ] **Step 6.4: Commit**

```bash
cd "C:/Users/miabs/mad-deployment-service/.worktrees/k8s-vuln-injection"
git add thesis/kubernetes-vulns/snippets/k02-*.yaml \
        thesis/kubernetes-vulns/kubernetes-vulns.tex
git commit -m "feat: add K02 snippets and LaTeX paragraphs"
```

---

### Task 7: K03 — Secrets Management Failures

**Files:**
- Create: `thesis/kubernetes-vulns/snippets/k03-*.yaml` (4 files)
- Modify: `thesis/kubernetes-vulns/kubernetes-vulns.tex` (append)

- [ ] **Step 7.1: Write K03 snippet files**

Create `thesis/kubernetes-vulns/snippets/k03-plaintextConfigMap-vuln.yaml`:
```yaml
# K03-a vulnerable: k03_plaintextConfigMapSecrets=true (pre-injected in MAD Goat)
# Database credentials in plaintext ConfigMap — readable by any pod in namespace
apiVersion: v1
kind: ConfigMap
metadata:
  name: mad-db-config
  namespace: default
data:
  POSTGRES_USER: "madgoat"
  POSTGRES_PASSWORD: "madgoat-dev-password"
  MONGO_INITDB_ROOT_USERNAME: "root"
  MONGO_INITDB_ROOT_PASSWORD: "example"
```

Create `thesis/kubernetes-vulns/snippets/k03-plaintextConfigMap-safe.yaml`:
```yaml
# K03-a remediated: k03_plaintextConfigMapSecrets=false
# Credentials migrated to a Kubernetes Secret (base64-encoded at rest)
apiVersion: v1
kind: Secret
metadata:
  name: mad-db-credentials
  namespace: default
type: Opaque
stringData:
  POSTGRES_USER: "madgoat"
  POSTGRES_PASSWORD: "madgoat-dev-password"
  MONGO_INITDB_ROOT_USERNAME: "root"
  MONGO_INITDB_ROOT_PASSWORD: "example"
```

Create `thesis/kubernetes-vulns/snippets/k03-jwtKeyInEnv-vuln.yaml`:
```yaml
# K03-b vulnerable: k03_jwtKeyInEnv=true (pre-injected in MAD Goat)
# JWT signing key exposed in ConfigMap — any principal can forge tokens
apiVersion: v1
kind: ConfigMap
metadata:
  name: mad-auth-config
  namespace: default
data:
  JWT_SECRET: "insecure-jwt-secret-for-dev"
```

Create `thesis/kubernetes-vulns/snippets/k03-jwtKeyInEnv-safe.yaml`:
```yaml
# K03-b remediated: k03_jwtKeyInEnv=false
# JWT key moved to a Secret; containers reference it via secretKeyRef
apiVersion: v1
kind: Secret
metadata:
  name: mad-jwt-secret
  namespace: default
type: Opaque
stringData:
  JWT_SECRET: "insecure-jwt-secret-for-dev"
```

- [ ] **Step 7.2: Append K03 LaTeX content**

Append to `thesis/kubernetes-vulns/kubernetes-vulns.tex`:

```latex

\subsubsection{K03 --- Secrets Management Failures (2022: K08)}
\label{sec:k03}

Kubernetes \texttt{ConfigMap} objects are stored unencrypted in \texttt{etcd} and are readable by any principal with \texttt{get} access to the namespace~\cite{k8s-secrets-docs}.
Both K03 entries were \emph{already present} in MAD~Goat before this extension (\texttt{presentPreInjection: true}); the flags retrofit a toggle-controlled migration path.

\paragraph{K03-a: Credentials in plaintext ConfigMap (CWE-522, CIS~5.4.1).}\label{sec:k03-plaintextConfigMap}
The \texttt{mad-db-config} ConfigMap stores PostgreSQL and MongoDB root credentials in plaintext.
Any pod that can execute \texttt{kubectl get configmap mad-db-config} or that mounts the ConfigMap as an environment source immediately obtains database credentials.
The flag \texttt{vulnerabilities.k03\_plaintextConfigMapSecrets=false} migrates these values to a \texttt{Secret}~\cite{k8s-secrets-docs,nsa-cisa-k8s-2022}.
Detected by: Trivy \texttt{AVD-KSV-0109}, KICS rule \texttt{3d658f8b}, Kubescape \texttt{C-0207}~\cite{trivy-k8s,kics-tool,kubescape}.

\begin{listing}[ht]
\inputminted[fontsize=\footnotesize,frame=lines]{yaml}{snippets/k03-plaintextConfigMap-vuln.yaml}
\caption{K03-a vulnerable: database credentials in a \texttt{ConfigMap}.}\label{lst:k03-plaintextConfigMap-vuln}
\end{listing}

\begin{listing}[ht]
\inputminted[fontsize=\footnotesize,frame=lines]{yaml}{snippets/k03-plaintextConfigMap-safe.yaml}
\caption{K03-a remediated: credentials moved to a \texttt{Secret}.}\label{lst:k03-plaintextConfigMap-safe}
\end{listing}

\paragraph{K03-b: JWT signing key in ConfigMap environment variable (CWE-522, CIS~5.4.2).}\label{sec:k03-jwtKeyInEnv}
The authentication service reads its HMAC JWT signing key from the \texttt{JWT\_SECRET} field of \texttt{mad-auth-config}.
An attacker with ConfigMap read access can forge arbitrary JWT tokens and impersonate any user.
Setting \texttt{vulnerabilities.k03\_jwtKeyInEnv=false} moves the key to a \texttt{Secret} referenced via \texttt{secretKeyRef}.
Detected by: Trivy \texttt{AVD-KSV-0109}, KICS rule \texttt{b9c83569}, Kubescape \texttt{C-0066}~\cite{trivy-k8s,kics-tool,kubescape}.

\begin{listing}[ht]
\inputminted[fontsize=\footnotesize,frame=lines]{yaml}{snippets/k03-jwtKeyInEnv-vuln.yaml}
\caption{K03-b vulnerable: JWT key in plaintext \texttt{ConfigMap}.}\label{lst:k03-jwtKeyInEnv-vuln}
\end{listing}

\begin{listing}[ht]
\inputminted[fontsize=\footnotesize,frame=lines]{yaml}{snippets/k03-jwtKeyInEnv-safe.yaml}
\caption{K03-b remediated: JWT key moved to a \texttt{Secret}.}\label{lst:k03-jwtKeyInEnv-safe}
\end{listing}
```

- [ ] **Step 7.3: Verify**

```bash
grep "label{sec:k03" thesis/kubernetes-vulns/kubernetes-vulns.tex
```

Expected: 3 lines

- [ ] **Step 7.4: Commit**

```bash
cd "C:/Users/miabs/mad-deployment-service/.worktrees/k8s-vuln-injection"
git add thesis/kubernetes-vulns/snippets/k03-*.yaml \
        thesis/kubernetes-vulns/kubernetes-vulns.tex
git commit -m "feat: add K03 snippets and LaTeX paragraphs"
```

---

### Task 8: K04 — Lack of Centralised Policy Enforcement

**Files:**
- Create: `thesis/kubernetes-vulns/snippets/k04-*.yaml` (4 files)
- Modify: `thesis/kubernetes-vulns/kubernetes-vulns.tex` (append)

- [ ] **Step 8.1: Write K04 snippet files**

Create `thesis/kubernetes-vulns/snippets/k04-noPSA-vuln.yaml`:
```yaml
# K04-a vulnerable: namespace labelled with PSA enforce=privileged
# Applied by: kubectl apply -k k8s/insecure-defaults/
# Any pod may run privileged — PSA enforcement disabled at namespace level
apiVersion: v1
kind: Namespace
metadata:
  name: default
  labels:
    pod-security.kubernetes.io/enforce: privileged
    pod-security.kubernetes.io/warn: privileged
    pod-security.kubernetes.io/audit: privileged
```

Create `thesis/kubernetes-vulns/snippets/k04-noPSA-safe.yaml`:
```yaml
# K04-a remediated: enforce=baseline, warn+audit=restricted
apiVersion: v1
kind: Namespace
metadata:
  name: default
  labels:
    pod-security.kubernetes.io/enforce: baseline
    pod-security.kubernetes.io/warn: restricted
    pod-security.kubernetes.io/audit: restricted
```

Create `thesis/kubernetes-vulns/snippets/k04-weakKyverno-vuln.yaml`:
```yaml
# K04-b vulnerable: Kyverno ClusterPolicy in Audit mode — violations logged, not blocked
# Applied by: kubectl apply -k k8s/insecure-defaults/
apiVersion: kyverno.io/v1
kind: ClusterPolicy
metadata:
  name: require-non-root
spec:
  validationFailureAction: Audit
  rules:
    - name: check-non-root
      match:
        resources:
          kinds: [Pod]
      validate:
        message: "Container must not run as root"
        pattern:
          spec:
            containers:
              - securityContext:
                  runAsNonRoot: true
```

Create `thesis/kubernetes-vulns/snippets/k04-weakKyverno-safe.yaml`:
```yaml
# K04-b remediated: validationFailureAction=Enforce — violations blocked at admission
apiVersion: kyverno.io/v1
kind: ClusterPolicy
metadata:
  name: require-non-root
spec:
  validationFailureAction: Enforce
  rules:
    - name: check-non-root
      match:
        resources:
          kinds: [Pod]
      validate:
        message: "Container must not run as root"
        pattern:
          spec:
            containers:
              - securityContext:
                  runAsNonRoot: true
```

- [ ] **Step 8.2: Append K04 LaTeX content**

Append to `thesis/kubernetes-vulns/kubernetes-vulns.tex`:

```latex

\subsubsection{K04 --- Lack of Centralised Policy Enforcement (2022: K04)}
\label{sec:k04}

Admission controllers are the last line of defence before a pod manifest reaches the scheduler.
Without enforcement-mode policies, all K01 misconfigurations can be deployed regardless of image scanner findings~\cite{k8s-psa-docs,kyverno-docs}.
Both K04 entries are cluster-level (\texttt{scope: cluster}) and are applied via \texttt{kubectl apply -k k8s/insecure-defaults/}.

\paragraph{K04-a: Namespace without Pod Security Admission enforcement (CWE-693, CIS~1.2.9).}\label{sec:k04-noPSA}
The \texttt{default} namespace is labelled with \texttt{pod-security.kubernetes.io/enforce: privileged}, which permits any pod security profile including \texttt{privileged: true}~\cite{k8s-psa-docs}.
This was already the Minikube default (\texttt{presentPreInjection: true}); the benchmark makes it explicit.
The exploit script \texttt{scripts/exploits/k04-noPSA.sh} verifies the label value.
Detected by: Trivy \texttt{KSV0010}, KICS rule \texttt{ce30e584}, Kubescape \texttt{C-0122}~\cite{trivy-k8s,kics-tool,kubescape}.

\begin{listing}[ht]
\inputminted[fontsize=\footnotesize,frame=lines]{yaml}{snippets/k04-noPSA-vuln.yaml}
\caption{K04-a vulnerable: \texttt{default} namespace enforces \texttt{privileged} PSA profile.}\label{lst:k04-noPSA-vuln}
\end{listing}

\begin{listing}[ht]
\inputminted[fontsize=\footnotesize,frame=lines]{yaml}{snippets/k04-noPSA-safe.yaml}
\caption{K04-a remediated: enforce \texttt{baseline}, audit \texttt{restricted}.}\label{lst:k04-noPSA-safe}
\end{listing}

\paragraph{K04-b: Kyverno installed with no-op \texttt{Audit} rules (CWE-693, CIS~1.2.12).}\label{sec:k04-weakKyverno}
Installing Kyverno with \texttt{validationFailureAction: Audit} creates the false impression of policy enforcement: violations are written to the Kyverno event log but non-compliant pods are admitted.
An operator inspecting \texttt{kubectl get clusterpolicy} would see active policies without realising they are non-blocking.
The benchmark applies the weak ClusterPolicy to demonstrate this false-positive governance pattern~\cite{kyverno-docs}.
Kubescape \texttt{C-0123} detects Audit-mode policies~\cite{kubescape}.

\begin{listing}[ht]
\inputminted[fontsize=\footnotesize,frame=lines]{yaml}{snippets/k04-weakKyverno-vuln.yaml}
\caption{K04-b vulnerable: \texttt{validationFailureAction: Audit} — violations logged but permitted.}\label{lst:k04-weakKyverno-vuln}
\end{listing}

\begin{listing}[ht]
\inputminted[fontsize=\footnotesize,frame=lines]{yaml}{snippets/k04-weakKyverno-safe.yaml}
\caption{K04-b remediated: \texttt{validationFailureAction: Enforce} — violations blocked.}\label{lst:k04-weakKyverno-safe}
\end{listing}
```

- [ ] **Step 8.3: Verify**

```bash
grep "label{sec:k04" thesis/kubernetes-vulns/kubernetes-vulns.tex
```

Expected: 3 lines

- [ ] **Step 8.4: Commit**

```bash
cd "C:/Users/miabs/mad-deployment-service/.worktrees/k8s-vuln-injection"
git add thesis/kubernetes-vulns/snippets/k04-*.yaml \
        thesis/kubernetes-vulns/kubernetes-vulns.tex
git commit -m "feat: add K04 snippets and LaTeX paragraphs"
```

---

### Task 9: K05 — Inadequate Network Segmentation

**Files:**
- Create: `thesis/kubernetes-vulns/snippets/k05-*.yaml` (6 files)
- Modify: `thesis/kubernetes-vulns/kubernetes-vulns.tex` (append)

- [ ] **Step 9.1: Write K05 snippet files**

Create `thesis/kubernetes-vulns/snippets/k05-noDefaultDeny-vuln.yaml`:
```yaml
# K05-a vulnerable: k05_noDefaultDeny=true (default)
# No default-deny NetworkPolicy — all pods can reach all other pods
# (Absence of the NetworkPolicy below is the vulnerability)
# Verify: kubectl get networkpolicy -n default | grep default-deny
```

Create `thesis/kubernetes-vulns/snippets/k05-noDefaultDeny-safe.yaml`:
```yaml
# K05-a remediated: k05_noDefaultDeny=false
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: default-deny-all
  namespace: default
spec:
  podSelector: {}
  policyTypes:
    - Ingress
    - Egress
```

Create `thesis/kubernetes-vulns/snippets/k05-permissiveNetpol-vuln.yaml`:
```yaml
# K05-b vulnerable: k05_permissiveNetpol=true (default)
# Broad label selector allows any pod labelled io.mad.service to reach any other
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: mad-network-policy
  namespace: default
spec:
  podSelector:
    matchLabels:
      io.mad.service: webapp
  ingress:
    - from:
        - podSelector:
            matchLabels:
              io.mad.service: webapp  # matches ALL MAD Goat pods
```

Create `thesis/kubernetes-vulns/snippets/k05-permissiveNetpol-safe.yaml`:
```yaml
# K05-b remediated: k05_permissiveNetpol=false
# Per-service rules: only traefik may reach webapp on port 8080
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: allow-webapp-ingress
  namespace: default
spec:
  podSelector:
    matchLabels:
      io.mad.service: webapp
  ingress:
    - from:
        - podSelector:
            matchLabels:
              io.mad.service: traefik
      ports:
        - protocol: TCP
          port: 8080
```

Create `thesis/kubernetes-vulns/snippets/k05-noEgressControls-vuln.yaml`:
```yaml
# K05-c vulnerable: k05_noEgressControls=true (default)
# No egress NetworkPolicy — pods can reach any external IP or internet endpoint
# Enables C2 callbacks, data exfiltration, and SSRF amplification
# (Absence of egress NetworkPolicy is the vulnerability)
```

Create `thesis/kubernetes-vulns/snippets/k05-noEgressControls-safe.yaml`:
```yaml
# K05-c remediated: k05_noEgressControls=false
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: restrict-egress
  namespace: default
spec:
  podSelector: {}
  policyTypes:
    - Egress
  egress:
    - to:
        - namespaceSelector:
            matchLabels:
              kubernetes.io/metadata.name: kube-system
      ports:
        - protocol: UDP
          port: 53  # DNS only
    - to:
        - podSelector:
            matchLabels:
              io.mad.service: postgres
      ports:
        - protocol: TCP
          port: 5432
```

- [ ] **Step 9.2: Append K05 LaTeX content**

Append to `thesis/kubernetes-vulns/kubernetes-vulns.tex`:

```latex

\subsubsection{K05 --- Inadequate Network Segmentation (2022: K07)}
\label{sec:k05}

Without \texttt{NetworkPolicy} objects the Kubernetes virtual network is fully flat: every pod can reach every other pod and any external endpoint~\cite{k8s-netpol-docs}.
MAD~Goat injects three complementary K05 sub-entries into \texttt{helm/madgoat-infra} that were all \emph{already present} in the pre-injection baseline (\texttt{presentPreInjection: true}).

\paragraph{K05-a: No default-deny \texttt{NetworkPolicy} (CWE-923, CIS~5.3.2).}\label{sec:k05-noDefaultDeny}
The absence of a deny-all baseline policy means that compromising any pod provides free lateral movement to every other pod without any network-layer control.
Setting \texttt{vulnerabilities.k05\_noDefaultDeny=false} adds the \texttt{default-deny-all} policy in Listing~\ref{lst:k05-noDefaultDeny-safe}.
Detected by: Trivy \texttt{KSV038}, KICS rule \texttt{03aabc8c}, KubeLinter \texttt{non-isolated-pod}, Kubescape \texttt{C-0205}~\cite{trivy-k8s,kics-tool,kubelinter,kubescape}.

\begin{listing}[ht]
\inputminted[fontsize=\footnotesize,frame=lines]{yaml}{snippets/k05-noDefaultDeny-vuln.yaml}
\caption{K05-a vulnerable: no default-deny \texttt{NetworkPolicy} present.}\label{lst:k05-noDefaultDeny-vuln}
\end{listing}

\begin{listing}[ht]
\inputminted[fontsize=\footnotesize,frame=lines]{yaml}{snippets/k05-noDefaultDeny-safe.yaml}
\caption{K05-a remediated: \texttt{default-deny-all} blocks all uninstructed traffic.}\label{lst:k05-noDefaultDeny-safe}
\end{listing}

\paragraph{K05-b: Overly permissive label-based \texttt{NetworkPolicy} (CWE-923, CIS~5.3.1).}\label{sec:k05-permissiveNetpol}
The pre-existing \texttt{mad-network-policy} uses a broad \texttt{podSelector} matching the \texttt{io.mad.service} label, which is shared by all MAD~Goat pods.
This effectively grants every service access to every other service, defeating the intent of the policy.
Setting \texttt{vulnerabilities.k05\_permissiveNetpol=false} replaces it with explicit per-service ingress rules.
Detected by: KICS rule \texttt{85ab1c5b}, KubeLinter \texttt{dangling-networkpolicy}, Kubescape \texttt{C-0206}~\cite{kics-tool,kubelinter,kubescape}.

\begin{listing}[ht]
\inputminted[fontsize=\footnotesize,frame=lines]{yaml}{snippets/k05-permissiveNetpol-vuln.yaml}
\caption{K05-b vulnerable: label-based policy permits all MAD~Goat pods to communicate.}\label{lst:k05-permissiveNetpol-vuln}
\end{listing}

\begin{listing}[ht]
\inputminted[fontsize=\footnotesize,frame=lines]{yaml}{snippets/k05-permissiveNetpol-safe.yaml}
\caption{K05-b remediated: explicit per-service ingress rule.}\label{lst:k05-permissiveNetpol-safe}
\end{listing}

\paragraph{K05-c: Missing egress controls (CWE-923, CIS~5.3.1).}\label{sec:k05-noEgressControls}
With no egress \texttt{NetworkPolicy}, any compromised pod can initiate outbound connections to arbitrary internet endpoints, enabling command-and-control callbacks, DNS exfiltration, or cloud-metadata queries~\cite{aws-imds}.
Setting \texttt{vulnerabilities.k05\_noEgressControls=false} adds an allowlist-based egress policy restricting outbound traffic to DNS and internal database services.
Detected by: Trivy \texttt{KSV038}, KICS rule \texttt{0401f71b}, KubeLinter \texttt{dangling-networkpolicypeer-podselector}, Kubescape \texttt{C-0054}~\cite{trivy-k8s,kics-tool,kubelinter,kubescape}.

\begin{listing}[ht]
\inputminted[fontsize=\footnotesize,frame=lines]{yaml}{snippets/k05-noEgressControls-vuln.yaml}
\caption{K05-c vulnerable: no egress \texttt{NetworkPolicy}.}\label{lst:k05-noEgressControls-vuln}
\end{listing}

\begin{listing}[ht]
\inputminted[fontsize=\footnotesize,frame=lines]{yaml}{snippets/k05-noEgressControls-safe.yaml}
\caption{K05-c remediated: egress allowlist restricts outbound to DNS and internal services.}\label{lst:k05-noEgressControls-safe}
\end{listing}
```

- [ ] **Step 9.3: Verify**

```bash
grep "label{sec:k05" thesis/kubernetes-vulns/kubernetes-vulns.tex
```

Expected: 4 lines

- [ ] **Step 9.4: Commit**

```bash
cd "C:/Users/miabs/mad-deployment-service/.worktrees/k8s-vuln-injection"
git add thesis/kubernetes-vulns/snippets/k05-*.yaml \
        thesis/kubernetes-vulns/kubernetes-vulns.tex
git commit -m "feat: add K05 snippets and LaTeX paragraphs"
```

---

### Task 10: K06 — Broken Authentication Mechanisms

**Files:**
- Create: `thesis/kubernetes-vulns/snippets/k06-*.yaml` (2 files)
- Modify: `thesis/kubernetes-vulns/kubernetes-vulns.tex` (append)

- [ ] **Step 10.1: Write K06 snippet files**

Create `thesis/kubernetes-vulns/snippets/k06-traefikDashboard-vuln.yaml`:
```yaml
# K06-a vulnerable: k06_traefikDashboardExposed=true (pre-injected)
# Traefik dashboard IngressRoute — no auth middleware, publicly accessible
apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: traefik-dashboard
  namespace: default
spec:
  entryPoints:
    - web
  routes:
    - match: "Host(`traefik.localhost`) && (PathPrefix(`/dashboard`) || PathPrefix(`/api`))"
      kind: Rule
      services:
        - name: api@internal
          kind: TraefikService
```

Create `thesis/kubernetes-vulns/snippets/k06-rabbitmqMgmt-vuln.yaml`:
```yaml
# K06-b vulnerable: k06_rabbitmqMgmtExposed=true (pre-injected)
# RabbitMQ management UI exposed via Traefik — default guest/guest credentials
apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: rabbitmq-management
  namespace: default
spec:
  entryPoints:
    - web
  routes:
    - match: "Host(`rabbitmq.localhost`)"
      kind: Rule
      services:
        - name: mad-rabbitmq
          port: 15672
```

- [ ] **Step 10.2: Append K06 LaTeX content**

Append to `thesis/kubernetes-vulns/kubernetes-vulns.tex`:

```latex

\subsubsection{K06 --- Broken Authentication Mechanisms (new in 2025)}
\label{sec:k06}

K06 is a new category in the 2025 edition with no direct 2022 equivalent.
It covers administrative interfaces and management planes that are exposed without authentication controls.
MAD~Goat injects two sub-entries into \texttt{helm/madgoat-infra}; both were present in the original deployment (\texttt{presentPreInjection: true}).

\paragraph{K06-a: Traefik dashboard publicly exposed (CWE-284).}\label{sec:k06-traefikDashboard}
Traefik ships with a dashboard and API at \texttt{/dashboard} and \texttt{/api} that expose the full routing configuration, middleware chain, and service health status.
The \texttt{IngressRoute} in Listing~\ref{lst:k06-traefikDashboard-vuln} routes external requests to the \texttt{api@internal} service with no authentication middleware~\cite{traefik-docs}.
An attacker can enumerate all routes, backends, and TLS certificates.
Setting \texttt{vulnerabilities.k06\_traefikDashboardExposed=false} suppresses the \texttt{IngressRoute} entirely.
No KICS/Trivy/KubeLinter rules cover Traefik-specific CRDs; Kubescape does not ship a rule for this pattern either — making it an example of an application-layer misconfiguration invisible to generic K8s scanners.

\begin{listing}[ht]
\inputminted[fontsize=\footnotesize,frame=lines]{yaml}{snippets/k06-traefikDashboard-vuln.yaml}
\caption{K06-a vulnerable: Traefik dashboard \texttt{IngressRoute} without authentication.}\label{lst:k06-traefikDashboard-vuln}
\end{listing}

\paragraph{K06-b: RabbitMQ management UI publicly exposed (CWE-284).}\label{sec:k06-rabbitmqMgmt}
RabbitMQ's management plugin exposes an HTTP API and web UI on port~15672 with default credentials \texttt{guest}/\texttt{guest}~\cite{traefik-docs}.
The \texttt{IngressRoute} in Listing~\ref{lst:k06-rabbitmqMgmt-vuln} routes \texttt{rabbitmq.localhost} directly to this port.
An unauthenticated attacker can enumerate queues, publish or consume messages, and obtain AMQP connection credentials.
Setting \texttt{vulnerabilities.k06\_rabbitmqMgmtExposed=false} removes the route; the management port remains internal-only.

\begin{listing}[ht]
\inputminted[fontsize=\footnotesize,frame=lines]{yaml}{snippets/k06-rabbitmqMgmt-vuln.yaml}
\caption{K06-b vulnerable: RabbitMQ management UI exposed on \texttt{rabbitmq.localhost}.}\label{lst:k06-rabbitmqMgmt-vuln}
\end{listing}
```

- [ ] **Step 10.3: Verify**

```bash
grep "label{sec:k06" thesis/kubernetes-vulns/kubernetes-vulns.tex
```

Expected: 3 lines

- [ ] **Step 10.4: Commit**

```bash
cd "C:/Users/miabs/mad-deployment-service/.worktrees/k8s-vuln-injection"
git add thesis/kubernetes-vulns/snippets/k06-*.yaml \
        thesis/kubernetes-vulns/kubernetes-vulns.tex
git commit -m "feat: add K06 snippets and LaTeX paragraphs"
```

---

### Task 11: K07 — Misconfigured Cluster Components (kubelet)

**Files:**
- Create: `thesis/kubernetes-vulns/snippets/k07-*.yaml` (2 files)
- Modify: `thesis/kubernetes-vulns/kubernetes-vulns.tex` (append)

- [ ] **Step 11.1: Write K07 snippet files**

Create `thesis/kubernetes-vulns/snippets/k07-kubelet-vuln.yaml`:
```yaml
# K07: kubelet started with anonymous auth + AlwaysAllow authorisation
# Enabled by: make reset-vulnerable
# Equivalent minikube flags:
#   --extra-config=kubelet.anonymous-auth=true
#   --extra-config=kubelet.authorization-mode=AlwaysAllow
apiVersion: kubelet.config.k8s.io/v1beta1
kind: KubeletConfiguration
authentication:
  anonymous:
    enabled: true      # unauthenticated requests accepted
  webhook:
    enabled: false
authorization:
  mode: AlwaysAllow    # every request is authorised unconditionally
```

Create `thesis/kubernetes-vulns/snippets/k07-kubelet-safe.yaml`:
```yaml
# K07 remediated: make reset-safe (no --extra-config kubelet flags)
# Minikube default — webhook authentication and webhook authorisation
apiVersion: kubelet.config.k8s.io/v1beta1
kind: KubeletConfiguration
authentication:
  anonymous:
    enabled: false
  webhook:
    enabled: true
    cacheTTL: "2m0s"
authorization:
  mode: Webhook
```

- [ ] **Step 11.2: Append K07 LaTeX content**

Append to `thesis/kubernetes-vulns/kubernetes-vulns.tex`:

```latex

\subsubsection{K07 --- Misconfigured Cluster Components (2022: K09)}
\label{sec:k07}

The kubelet is the per-node agent that executes pod manifests; its HTTP API exposes pod logs, \texttt{exec} endpoints, and node metrics.
K07 kubelet entries are cluster-level and not Helm-controlled: they require \texttt{minikube start -{}-extra-config} flags and are provisioned by \texttt{make reset-vulnerable}~\cite{k8s-kubelet-docs}.

\paragraph{K07-a: \texttt{kubelet} anonymous authentication enabled (CWE-306, CIS~1.2.1).}\label{sec:k07-kubeletAnonymousAuth}
Setting \texttt{authentication.anonymous.enabled=true} instructs the kubelet to accept unauthenticated requests as the \texttt{system:anonymous} user.
An attacker with network access to port~10250 can retrieve pod logs, environment variables, and container file system listings without credentials.
The exploit script \texttt{scripts/exploits/k07-kubeletAnonymousAuth.sh} confirms the condition by querying the kubelet \texttt{/healthz} endpoint without a token.
Detected by: Trivy \texttt{KCV0001}, KICS rule \texttt{1de5cc51}, Kubescape \texttt{C-0113}~\cite{trivy-k8s,kics-tool,kubescape}.

\begin{listing}[ht]
\inputminted[fontsize=\footnotesize,frame=lines]{yaml}{snippets/k07-kubelet-vuln.yaml}
\caption{K07 vulnerable: kubelet with \texttt{anonymous-auth=true} and \texttt{AlwaysAllow} authorisation.}\label{lst:k07-kubelet-vuln}
\end{listing}

\paragraph{K07-b: \texttt{kubelet} authorisation mode set to \texttt{AlwaysAllow} (CWE-306, CIS~1.2.6).}\label{sec:k07-kubeletAlwaysAllow}
Even when authentication is enabled, \texttt{authorization.mode=AlwaysAllow} grants every authenticated identity full access to every kubelet API endpoint, including \texttt{/run} (arbitrary command execution on the node).
This is combined with K07-a in \texttt{make reset-vulnerable} to produce a fully open kubelet.
Detected by: Trivy \texttt{KCV0007}, KICS rule \texttt{f1f4d8da}, Kubescape \texttt{C-0120}~\cite{trivy-k8s,kics-tool,kubescape}.

\begin{listing}[ht]
\inputminted[fontsize=\footnotesize,frame=lines]{yaml}{snippets/k07-kubelet-safe.yaml}
\caption{K07 remediated: webhook authentication and authorisation enforced by \texttt{make reset-safe}.}\label{lst:k07-kubelet-safe}
\end{listing}
```

- [ ] **Step 11.3: Verify**

```bash
grep "label{sec:k07" thesis/kubernetes-vulns/kubernetes-vulns.tex
```

Expected: 3 lines

- [ ] **Step 11.4: Commit**

```bash
cd "C:/Users/miabs/mad-deployment-service/.worktrees/k8s-vuln-injection"
git add thesis/kubernetes-vulns/snippets/k07-*.yaml \
        thesis/kubernetes-vulns/kubernetes-vulns.tex
git commit -m "feat: add K07 snippets and LaTeX paragraphs"
```

---

### Task 12: K08 — Supply Chain Vulnerabilities (conceptual)

**Files:**
- Modify: `thesis/kubernetes-vulns/kubernetes-vulns.tex` (append only — no snippets)

- [ ] **Step 12.1: Append K08 LaTeX content**

Append to `thesis/kubernetes-vulns/kubernetes-vulns.tex`:

```latex

\subsubsection{K08 --- Supply Chain Vulnerabilities (2022: K02)}
\label{sec:k08}

K08 covers two attack paths that exploit trust in the supply chain of a running cluster.
Both entries are classified \texttt{class: conceptual} because their prerequisites (cloud-provider IMDS, managed Kubernetes node IAM roles) do not exist in a local Minikube cluster.
They are documented here as design patterns with cloud-environment references~\cite{aws-imds,azure-imds}.

\paragraph{K08-a: IMDS credential theft via SSRF (conceptual).}\label{sec:k08-imdsCredentialTheft}
In cloud-hosted Kubernetes clusters (EKS, AKS, GKE), the Instance Metadata Service (IMDS) is reachable at \texttt{169.254.169.254} from within any pod unless explicitly blocked.
An SSRF vulnerability in any MAD~Goat service can be chained to a request to \texttt{http://169.254.169.254/latest/meta-data/iam/security-credentials/} to obtain temporary AWS IAM credentials for the node's instance profile.
The absence of K05-c egress controls (Section~\ref{sec:k05-noEgressControls}) enables this attack path.
Mitigation requires IMDSv2 enforcement (\texttt{HttpTokens: required}) and an egress \texttt{NetworkPolicy} blocking \texttt{169.254.0.0/16}.

\paragraph{K08-b: Service-account token projection to cloud (conceptual).}\label{sec:k08-saTokenToCloud}
Kubernetes service-account tokens projected into pods via the \texttt{BoundServiceAccountTokenVolume} feature can be exchanged for cloud-provider credentials using OIDC workload identity federation.
Combined with the K09-b shared default ServiceAccount (Section~\ref{sec:k09-sharedServiceAccount}), a compromised pod can present its token to the cloud IAM endpoint and assume the node's workload identity.
Mitigation requires per-workload ServiceAccounts with explicit OIDC audience bindings and shortest-possible token TTLs.
```

- [ ] **Step 12.2: Verify**

```bash
grep "label{sec:k08" thesis/kubernetes-vulns/kubernetes-vulns.tex
```

Expected: 3 lines

- [ ] **Step 12.3: Commit**

```bash
cd "C:/Users/miabs/mad-deployment-service/.worktrees/k8s-vuln-injection"
git add thesis/kubernetes-vulns/kubernetes-vulns.tex
git commit -m "feat: add K08 conceptual LaTeX paragraphs"
```

---

### Task 13: K09 — Misconfigured Authentication Defaults

**Files:**
- Create: `thesis/kubernetes-vulns/snippets/k09-*.yaml` (4 files)
- Modify: `thesis/kubernetes-vulns/kubernetes-vulns.tex` (append)

- [ ] **Step 13.1: Write K09 snippet files**

Create `thesis/kubernetes-vulns/snippets/k09-keycloak-vuln.yaml`:
```yaml
# K09-a vulnerable: k09_anonymousKeycloakBootstrap=true (pre-injected)
# Keycloak running in start-dev mode: no HTTPS, embedded H2 DB, admin/admin
spec:
  containers:
    - name: mad-keycloak
      args:
        - "start-dev"   # development mode — admin console unauthenticated over HTTP
      env:
        - name: KEYCLOAK_ADMIN
          value: "admin"
        - name: KEYCLOAK_ADMIN_PASSWORD
          value: "admin"
```

Create `thesis/kubernetes-vulns/snippets/k09-keycloak-safe.yaml`:
```yaml
# K09-a remediated: k09_anonymousKeycloakBootstrap=false
# Switches to production 'start' command; admin console requires credentials
spec:
  containers:
    - name: mad-keycloak
      args:
        - "start"
        - "--hostname-strict=false"
        - "--http-enabled=true"
      env:
        - name: KEYCLOAK_ADMIN
          value: "admin"
        - name: KEYCLOAK_ADMIN_PASSWORD
          value: "admin"
```

Create `thesis/kubernetes-vulns/snippets/k09-sharedSA-vuln.yaml`:
```yaml
# K09-b vulnerable: k09_sharedServiceAccount=true (default)
# All pods use the 'default' ServiceAccount — token auto-mounted and shared
spec:
  serviceAccountName: default
  automountServiceAccountToken: true  # implicit default
```

Create `thesis/kubernetes-vulns/snippets/k09-sharedSA-safe.yaml`:
```yaml
# K09-b remediated: k09_sharedServiceAccount=false
# Per-service ServiceAccount created with automount disabled
apiVersion: v1
kind: ServiceAccount
metadata:
  name: sa-webapp
  namespace: default
automountServiceAccountToken: false
---
# Deployment references dedicated SA
spec:
  serviceAccountName: sa-webapp
  automountServiceAccountToken: false
```

- [ ] **Step 13.2: Append K09 LaTeX content**

Append to `thesis/kubernetes-vulns/kubernetes-vulns.tex`:

```latex

\subsubsection{K09 --- Misconfigured Authentication Defaults (2022: K06)}
\label{sec:k09}

K09 groups authentication defaults in cluster components and workloads that allow unauthenticated or weakly authenticated access.
MAD~Goat injects two K09 sub-entries into \texttt{helm/madgoat-infra} and both charts respectively.

\paragraph{K09-a: Keycloak default admin credentials and \texttt{start-dev} mode (CWE-200).}\label{sec:k09-anonymousKeycloakBootstrap}
Keycloak's \texttt{start-dev} command disables HTTPS, uses an embedded H2 database, and starts with default credentials \texttt{admin}/\texttt{admin}~\cite{keycloak-docs}.
This was already the MAD~Goat deployment configuration (\texttt{presentPreInjection: true}).
The flag \texttt{vulnerabilities.k09\_anonymousKeycloakBootstrap=false} switches the Keycloak container command to \texttt{start} (production mode), enforcing HTTPS and preventing unauthenticated admin console access.
No generic scanner rules exist for application-level credential defaults; the exploit script \texttt{scripts/exploits/k09-anonymousKeycloakBootstrap.sh} verifies HTTP 200 from the admin console endpoint without credentials.

\begin{listing}[ht]
\inputminted[fontsize=\footnotesize,frame=lines]{yaml}{snippets/k09-keycloak-vuln.yaml}
\caption{K09-a vulnerable: Keycloak in \texttt{start-dev} with default \texttt{admin}/\texttt{admin}.}\label{lst:k09-keycloak-vuln}
\end{listing}

\begin{listing}[ht]
\inputminted[fontsize=\footnotesize,frame=lines]{yaml}{snippets/k09-keycloak-safe.yaml}
\caption{K09-a remediated: Keycloak in production \texttt{start} mode.}\label{lst:k09-keycloak-safe}
\end{listing}

\paragraph{K09-b: All pods share the \texttt{default} ServiceAccount (CWE-200).}\label{sec:k09-sharedServiceAccount}
When no explicit \texttt{serviceAccountName} is set, Kubernetes assigns the \texttt{default} ServiceAccount, which auto-mounts a JWT token into every pod at \texttt{/var/run/secrets/kubernetes.io/serviceaccount/token}~\cite{k8s-rbac-docs}.
Combined with K02-a (cluster-admin binding on \texttt{default}), any compromised pod can use this token to control the cluster.
Even without an elevated binding, a shared token increases the blast radius of any pod compromise.
The flag \texttt{vulnerabilities.k09\_sharedServiceAccount=false} creates a dedicated per-service \texttt{ServiceAccount} with \texttt{automountServiceAccountToken: false} for every service in both charts.
Kubescape \texttt{C-0183} detects automounted tokens~\cite{kubescape}.

\begin{listing}[ht]
\inputminted[fontsize=\footnotesize,frame=lines]{yaml}{snippets/k09-sharedSA-vuln.yaml}
\caption{K09-b vulnerable: all pods share the \texttt{default} SA with auto-mounted token.}\label{lst:k09-sharedSA-vuln}
\end{listing}

\begin{listing}[ht]
\inputminted[fontsize=\footnotesize,frame=lines]{yaml}{snippets/k09-sharedSA-safe.yaml}
\caption{K09-b remediated: per-service \texttt{ServiceAccount} with \texttt{automountServiceAccountToken: false}.}\label{lst:k09-sharedSA-safe}
\end{listing}
```

- [ ] **Step 13.3: Verify**

```bash
grep "label{sec:k09" thesis/kubernetes-vulns/kubernetes-vulns.tex
```

Expected: 3 lines

- [ ] **Step 13.4: Commit**

```bash
cd "C:/Users/miabs/mad-deployment-service/.worktrees/k8s-vuln-injection"
git add thesis/kubernetes-vulns/snippets/k09-*.yaml \
        thesis/kubernetes-vulns/kubernetes-vulns.tex
git commit -m "feat: add K09 snippets and LaTeX paragraphs"
```

---

### Task 14: K10 — Inadequate Audit Logging

**Files:**
- Create: `thesis/kubernetes-vulns/snippets/k10-auditPolicy-exploit.sh`
- Create: `thesis/kubernetes-vulns/snippets/k10-auditPolicy-safe.yaml`
- Modify: `thesis/kubernetes-vulns/kubernetes-vulns.tex` (append)

- [ ] **Step 14.1: Write K10 snippet files**

Create `thesis/kubernetes-vulns/snippets/k10-auditPolicy-exploit.sh`:
```bash
#!/bin/bash
# Verify absence of kube-apiserver audit policy (K10)
set -e
AUDIT_FLAG=$(minikube ssh -- \
  "ps aux | grep kube-apiserver | grep -o 'audit-policy-file=[^ ]*' 2>/dev/null || true")
if [ -z "$AUDIT_FLAG" ]; then
  echo "PASS: No audit-policy-file — API audit logging absent (vulnerable)"; exit 0
else
  echo "FAIL: audit-policy-file=$AUDIT_FLAG — safe mode active"; exit 1
fi
```

Create `thesis/kubernetes-vulns/snippets/k10-auditPolicy-safe.yaml`:
```yaml
# K10 remediated: audit policy applied via minikube cp + apiserver restart
# Captures: secret access, pod exec/attach, RBAC changes, auth failures
apiVersion: audit.k8s.io/v1
kind: Policy
rules:
  - level: RequestResponse
    resources:
    - group: ""
      resources: ["secrets"]
  - level: RequestResponse
    verbs: ["create"]
    resources:
    - group: ""
      resources: ["pods/exec", "pods/attach", "pods/portforward"]
  - level: RequestResponse
    resources:
    - group: "rbac.authorization.k8s.io"
      resources: ["clusterrolebindings", "rolebindings", "clusterroles", "roles"]
  - level: Metadata
    omitStages:
      - RequestReceived
  - level: None
    users: ["system:kube-proxy"]
    verbs: ["watch"]
    resources:
    - group: ""
      resources: ["endpoints", "services", "services/status"]
```

- [ ] **Step 14.2: Make exploit snippet executable**

```bash
chmod +x thesis/kubernetes-vulns/snippets/k10-auditPolicy-exploit.sh
```

- [ ] **Step 14.3: Append K10 LaTeX content**

Append to `thesis/kubernetes-vulns/kubernetes-vulns.tex`:

```latex

\subsubsection{K10 --- Inadequate Audit Logging (2022: K05)}
\label{sec:k10}

The Kubernetes API server can emit structured audit events for every API call, but this feature is disabled by default~\cite{k8s-audit-docs}.
Without an audit policy, there is no forensic record of secret access, privilege escalation, or lateral movement.

\paragraph{K10-a: No \texttt{kube-apiserver} audit policy configured (CWE-778, CIS~1.2.16).}\label{sec:k10-noAuditPolicy}
Minikube starts the API server without \texttt{-{}-audit-policy-file}, \texttt{-{}-audit-log-path}, or related flags.
All API operations are silent: an attacker who lists all Secrets, binds themselves to \texttt{cluster-admin}, and exfiltrates data leaves no server-side log~\cite{k8s-audit-docs}.
This entry is cluster-level and was \emph{already present} in the baseline (\texttt{presentPreInjection: true}).
The exploit script (Listing~\ref{lst:k10-auditPolicy-exploit}) confirms the absence by inspecting the running \texttt{kube-apiserver} process arguments.
Safe mode requires copying \texttt{k8s/insecure-defaults/audit-policy-safe.yaml} to the Minikube node and restarting the API server with the audit flags (see \texttt{audit-policy-missing.md}).
Detected by: Trivy \texttt{KCV0019}, KICS rule \texttt{13a49a2e}, Kubescape \texttt{C-0130}~\cite{trivy-k8s,kics-tool,kubescape}.

\begin{listing}[ht]
\inputminted[fontsize=\footnotesize,frame=lines]{bash}{snippets/k10-auditPolicy-exploit.sh}
\caption{K10-a exploit script: confirms absence of \texttt{-{}-audit-policy-file} on running API server.}\label{lst:k10-auditPolicy-exploit}
\end{listing}

\begin{listing}[ht]
\inputminted[fontsize=\footnotesize,frame=lines]{yaml}{snippets/k10-auditPolicy-safe.yaml}
\caption{K10-a remediated: audit policy capturing secrets access, pod exec, RBAC changes, and auth failures.}\label{lst:k10-auditPolicy-safe}
\end{listing}
```

- [ ] **Step 14.4: Verify**

```bash
grep "label{sec:k10" thesis/kubernetes-vulns/kubernetes-vulns.tex
```

Expected: 2 lines

- [ ] **Step 14.5: Commit**

```bash
cd "C:/Users/miabs/mad-deployment-service/.worktrees/k8s-vuln-injection"
git add thesis/kubernetes-vulns/snippets/k10-auditPolicy-exploit.sh \
        thesis/kubernetes-vulns/snippets/k10-auditPolicy-safe.yaml \
        thesis/kubernetes-vulns/kubernetes-vulns.tex
git commit -m "feat: add K10 snippets and LaTeX paragraphs"
```

---

### Task 15: Discussion, Summary, scanner table, and compilation test

**Files:**
- Modify: `thesis/kubernetes-vulns/kubernetes-vulns.tex` (append closing sections)

- [ ] **Step 15.1: Append Discussion and Summary to kubernetes-vulns.tex**

Append to `thesis/kubernetes-vulns/kubernetes-vulns.tex`:

```latex

\subsection{Discussion}
\label{sec:k8s-discussion}

\paragraph{Outdated Kubernetes components (K10-related).}\label{sec:k8s-outdatedComponents}
Zerouali et al.~\cite{zerouali-2023} demonstrate that a significant fraction of Docker images in the wild carry outdated system packages.
The same phenomenon applies to the Kubernetes version itself: Minikube ships with a fixed Kubernetes release that accumulates \textsc{cve}s over time.
MAD~Goat does not inject a specific outdated-components vulnerability because the benchmark's Minikube target version is already configurable (\texttt{minikube start -{}-kubernetes-version}); however, this axis is a natural extension for future benchmark releases.

\paragraph{Log aggregation gap (K10-related).}\label{sec:k8s-logAggregation}
The K10-a audit policy (Section~\ref{sec:k10-noAuditPolicy}) writes events to a local file on the Minikube node.
A production deployment requires shipping these events to a central SIEM (e.g., Elasticsearch, Splunk) to survive node failure and support cross-cluster correlation.
The benchmark documents this gap in \texttt{k8s/insecure-defaults/audit-policy-missing.md} but defers log-shipping configuration to the operator, as SIEM integration is out of scope for a local benchmark cluster.

\paragraph{Scanner coverage gaps.}\label{sec:k8s-scannerGaps}
Table~\ref{tab:scanner-coverage} reveals that application-layer misconfigurations (K06-a Traefik dashboard, K06-b RabbitMQ management, K09-a Keycloak start-dev) produce no findings in any of the four tested scanners.
This is consistent with findings by Bose et al.~\cite{bose-2023} that a class of ``under-reported'' Kubernetes security defects exists outside the scope of manifest-level static analysis.
These entries require application-aware or behavioural detection methods, such as network policy egress anomaly detection or custom \textsc{opa}/Kyverno rules tuned to the specific service.

\subsection{Summary}
\label{sec:k8s-summary}

This section has extended the MAD~Goat benchmark with 20 toggle-controlled vulnerability entries spanning all ten OWASP Kubernetes Top~10 (2025) categories.
Fifteen entries are implemented via Helm chart boolean flags in \texttt{helm/madgoat} and \texttt{helm/madgoat-infra}; five cluster-level entries are applied via \texttt{kubectl apply -k k8s/insecure-defaults/} and Minikube startup flags.
Each entry ships with a verification script in \texttt{scripts/exploits/}, and a \texttt{Makefile} target (\texttt{make reset-vulnerable} / \texttt{make reset-safe}) provisions the full vulnerable or hardened cluster state.
Two entries (K08) are conceptual-only, reflecting the cloud-specific nature of IMDS and workload-identity attacks.
Table~\ref{tab:scanner-coverage} quantifies the detection gap: of the four scanners evaluated, none detect application-layer authentication defaults (K06, K09-a), underscoring the need for application-aware policy rules as a complement to generic manifest scanning~\cite{bose-2023,rahman-2019}.

\input{tables/scanner-coverage}
```

- [ ] **Step 15.2: Close the section file**

Append the closing brace of `\subsection{Vulnerability Catalogue}` is already open — the file needs no extra brace; the section ends naturally with the last `\input`. Verify structure:

```bash
grep "^\\\\subsection\|^\\\\subsubsection\|^\\\\section" thesis/kubernetes-vulns/kubernetes-vulns.tex
```

Expected lines (in order):
```
\section{Extending the MAD Goat Benchmark to the Kubernetes Layer}
\subsection{Motivation and Context}
\subsection{Methodology}
\subsection{Vulnerability Catalogue}
\subsubsection{K01
\subsubsection{K02
\subsubsection{K03
\subsubsection{K04
\subsubsection{K05
\subsubsection{K06
\subsubsection{K07
\subsubsection{K08
\subsubsection{K09
\subsubsection{K10
\subsection{Discussion}
\subsection{Summary}
```

- [ ] **Step 15.3: Run gen-tables.sh to ensure tables are current**

```bash
cd "C:/Users/miabs/mad-deployment-service/.worktrees/k8s-vuln-injection"
bash thesis/kubernetes-vulns/scripts/gen-tables.sh
```

Expected:
```
Generated: .../tables/vuln-inventory.tex
Generated: .../tables/scanner-coverage.tex
```

- [ ] **Step 15.4: Compile with pdflatex**

```bash
cd "C:/Users/miabs/mad-deployment-service/.worktrees/k8s-vuln-injection/thesis/kubernetes-vulns"
pdflatex -shell-escape -interaction=nonstopmode test-main.tex 2>&1 | tail -20
pdflatex -shell-escape -interaction=nonstopmode test-main.tex 2>&1 | grep -E "^!" | head -5
```

Run pdflatex **twice** (second pass resolves cross-references).

Expected after second pass: no lines starting with `!` (no LaTeX errors). Warnings about undefined references are acceptable on first standalone compile.

- [ ] **Step 15.5: Verify PDF exists**

```bash
ls -lh thesis/kubernetes-vulns/test-main.pdf
```

Expected: file exists, size > 50 KB

- [ ] **Step 15.6: Count labels**

```bash
grep -c "\\\\label" thesis/kubernetes-vulns/kubernetes-vulns.tex
```

Expected: ≥ 45 (section, subsection, subsubsection, paragraph, listing labels)

- [ ] **Step 15.7: Add _minted cache to .gitignore**

```bash
grep "_minted" .gitignore || echo "_minted*" >> .gitignore
```

- [ ] **Step 15.8: Commit everything**

```bash
cd "C:/Users/miabs/mad-deployment-service/.worktrees/k8s-vuln-injection"
git add thesis/kubernetes-vulns/kubernetes-vulns.tex \
        thesis/kubernetes-vulns/tables/vuln-inventory.tex \
        thesis/kubernetes-vulns/tables/scanner-coverage.tex \
        .gitignore
git commit -m "feat: complete kubernetes-vulns.tex — discussion, summary, and passing pdflatex compile"
```

---

## Self-Review

### Spec coverage

| Requirement | Task |
|---|---|
| `gen-tables.sh` (yq + awk, Tables 2 & 3) | Task 1 |
| Table 1 crosswalk 2022↔2025 (static LaTeX) | Task 2 |
| `references.bib` (~30 IEEE BibTeX) | Task 3 |
| `kubernetes-vulns.tex` opening (motivation + methodology) | Task 4 |
| K01 × 4 sub-entries with snippets | Task 5 |
| K02 × 2 sub-entries with snippets | Task 6 |
| K03 × 2 sub-entries with snippets | Task 7 |
| K04 × 2 sub-entries with snippets | Task 8 |
| K05 × 3 sub-entries with snippets | Task 9 |
| K06 × 2 sub-entries with snippets | Task 10 |
| K07 × 2 sub-entries with snippets | Task 11 |
| K08 × 2 conceptual entries (prose only) | Task 12 |
| K09 × 2 sub-entries with snippets | Task 13 |
| K10 × 1 full entry with snippets | Task 14 |
| Discussion + Summary + compilation | Task 15 |
| `\label` on every `\paragraph` | All K tasks |
| `\inputminted` for all snippets | All K tasks |
| Scanner coverage per entry cited | All K tasks |

### Placeholder scan

No TBD, TODO, "implement later", "add appropriate", "similar to task N", or unexplained type references found.

### Type consistency

- All listing labels follow pattern `lst:kXX-slug-vuln` / `lst:kXX-slug-safe`
- All section labels follow pattern `sec:kXX` / `sec:kXX-slug`
- All snippet filenames follow pattern `kXX-slug-vuln.yaml` / `kXX-slug-safe.yaml`
- `\ref{lst:...}` calls in paragraphs match labels defined in same task
- BibTeX keys used in cite commands (`owasp-k8s-2025`, `cis-k8s-1.9`, etc.) all defined in Task 3 `references.bib`
