{{- range .Values.configMaps }}
---
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ .name }}
data:
  {{ range $envKey, $envValue := .data }}
  {{ $envKey }}: "{{ $envValue }}"
  {{- end }}
{{- end}}