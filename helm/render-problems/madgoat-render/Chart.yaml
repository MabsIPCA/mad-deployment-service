apiVersion: v2
name: madgoat-render
description: MADGoat app chart — render-error variant for fixer testing
type: application
version: 0.1.0
dependencies:
  - name: madgoat-infra-render
    version: 0.1.0
    repository: "file://../madgoat-infra-render"
# r05_kubeVersion is not toggleable via values; set kubeVersion here directly
# when you need to exercise the kube_version fixer path, e.g.:
kubeVersion: ">= 1.99.0 < 2.0.0"
